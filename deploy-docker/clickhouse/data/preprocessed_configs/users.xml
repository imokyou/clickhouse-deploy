<!-- This file was generated automatically.
     Do not edit it: it is likely to be discarded and generated again before it's read next time.
     Files used to generate this file:
       /etc/clickhouse-server/users.xml
       /etc/clickhouse-server/users.d/default-user.xml
       /etc/clickhouse-server/users.d/users.xml      -->

<clickhouse>
    <!-- See also the files in users.d directory where the settings can be overridden. -->

    <!-- Profiles of settings. -->
    <profiles>
        <!-- Default settings. -->
        <default>
            <!-- <async_insert>1</async_insert> -->
        </default>

        <!-- Profile that allows only read queries. -->
        <readonly>
            <readonly>1</readonly>
        </readonly>
    </profiles>

    <!-- Users and ACL. -->
    <users>
        <!-- If user name was not specified, 'default' user is used. -->
        
    
    <!-- Remove default user -->
    

    <default>
      <profile>default</profile>
      <networks>
        <ip>::/0</ip>
      </networks>
      <password><![CDATA[clickhouse123]]></password>
      <quota>default</quota>
      <access_management>1</access_management>
    
            
            <max_threads>4</max_threads>
            <max_insert_threads>4</max_insert_threads>
            <max_concurrent_queries>10</max_concurrent_queries>
            <max_memory_usage>12884901888</max_memory_usage>
            <max_memory_usage_for_user>12884901888</max_memory_usage_for_user>
            <max_memory_usage_for_all_queries>12884901888</max_memory_usage_for_all_queries>
            <max_memory_usage_for_user_for_all_queries>12884901888</max_memory_usage_for_user_for_all_queries>

            <!-- Timeouts -->
            <connect_timeout>10</connect_timeout>
            <receive_timeout>300</receive_timeout>
            <send_timeout>300</send_timeout>

            <!-- Async inserts -->
            <async_insert>1</async_insert>
            <async_insert_threads>4</async_insert_threads>
        </default>
  
        

        <!-- New application user -->
        <appuser>
            <password>ChangeMe_123!</password>
            <networks>
                <!-- Restrict in production: e.g. 10.0.0.0/8 or specific IPs -->
                <ip>::/0</ip>
            </networks>
            <!-- Use default profile/quotas; optional to customize -->
            <grants>
                <!-- Per docs, do not specify grantee here -->
                <query>GRANT SELECT, INSERT, CREATE, ALTER, DROP, DELETE ON *.*</query>
            </grants>
        </appuser>
    </users>

    <!-- Quotas. -->
    <quotas>
        <!-- Name of quota. -->
        <default>
            <!-- Limits for time interval. You could specify many intervals with different limits. -->
            <interval>
                <!-- Length of interval. -->
                <duration>3600</duration>

                <!-- No limits. Just calculate resource usage for time interval. -->
                <queries>0</queries>
                <errors>0</errors>
                <result_rows>0</result_rows>
                <read_rows>0</read_rows>
                <execution_time>0</execution_time>
            </interval>
        </default>
    </quotas>

  <!-- Docs: <https://clickhouse.com/docs/operations/settings/settings_users/> -->
  

    
</clickhouse>
